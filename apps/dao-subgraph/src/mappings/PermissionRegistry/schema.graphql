type Permission @entity {
  id: ID!
  from: String!
  to: String!
  functionSignature: String!
  valueAllowed: BigInt!
  fromTime: BigInt!
  allowed: Boolean!

  # TODO: Makes sense to compute these?
  # valueTransferred: BigInt
  # valueTransferredOnBlock: BigInt
}

