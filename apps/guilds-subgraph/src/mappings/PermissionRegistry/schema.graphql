type GuildPermission @entity {
  id: ID!
  from: String!
  to: String!
  functionSignature: Bytes!
  isToken: Boolean!
  valueAllowed: BigInt!
  fromTime: BigInt!
  allowed: Boolean!
  guild: Guild
}

